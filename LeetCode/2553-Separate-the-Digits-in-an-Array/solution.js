class Solution {
public:
    vector<int> separateDigits(vector<int>& nums) {
        vector<int> ans;
        for(int x:nums)
        {
            while(x!=0)
        }
            {
                int d=x%10;
            }
    }
                a.push_back(d);
                x/=10;
        return ans;

            vector<int> a;
            for(auto x=rend()
};
